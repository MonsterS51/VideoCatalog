<UserControl x:Class="VideoCatalog.Windows.PreviewFrameLibVLC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
             xmlns:local="clr-namespace:VideoCatalog.Windows"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid SnapsToDevicePixels="True" UseLayoutRounding="True" >
		<vlc:VideoView x:Name="mediaPlayer" Visibility="Visible" IsEnabled="False">
			<Grid>
				<ProgressBar x:Name="prevProgress" Maximum="10" Value="5" Visibility="Hidden"
						Height="3.5" HorizontalAlignment="Stretch" VerticalAlignment="Top" BorderThickness="0" BorderBrush="#00000000"
						Margin="0,0,0,0" Padding="0,0,0,0" Background="#CC000000" Foreground="#FF00B4FF">
				</ProgressBar>
				<Rectangle x:Name="playerRect" MouseEnter="playerRect_MouseEnter" Fill="#02000000" MouseLeave="playerRect_MouseLeave"/>
			</Grid>
		</vlc:VideoView>
		



	</Grid>
</UserControl>
